<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>My_Opportunities__c</fullName>
    <description>This formula field is used for dynamic reporting, so any Trader can view a single (configured) report and the data will only return results where they are named in the Trader field.</description>
    <externalId>false</externalId>
    <formula>IF(
$User.Id = Trader__r.Id
,true,false)</formula>
    <inlineHelpText>This formula field is used for dynamic reporting, so any Trader can view a single (configured) report and the data will only return results where they are named in the Trader field.</inlineHelpText>
    <label>My Opportunities</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
